# Allintos / Chatbot Bilgi Merkezi — proje geneli Python bağımlılıkları
# (chatbot_demo + Allintos backend + veritabani test senaryoları)
#
# Kurulum (proje kökünden):
#   pip install -r requirements.txt

# ── Chatbot API (FastAPI) ───────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0

# ── Allintos web backend (Flask) ────────────────────────────
Flask==3.0.3
flask-cors>=6.0.1

# ── PostgreSQL + ORM + migration ────────────────────────────
sqlalchemy>=2.0.38
alembic>=1.14.0
psycopg2-binary>=2.9.10
pgvector>=0.3.6

# ── Ortam / güvenlik ────────────────────────────────────────
python-dotenv>=1.0.1
bcrypt>=4.2.1

# ── PDF / medya (Allintos backend) ──────────────────────────
reportlab>=4.4.3
xhtml2pdf==0.2.17
Pillow>=12.3.0,<13.0

# ── BGE-M3 embedding (chatbot — app/services/embedder.py) ───
numpy>=1.24.0
torch>=2.0.0
FlagEmbedding>=1.2.0
