# ── Python / ortam ──────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
dist/
build/
.venv/
venv/
.env

# Test / lint / coverage cache
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
*.log

# ── Çalışma zamanında kullanılmayan geliştirme dosyaları ───
tests/
_final_dogrulama.py
scratch/
reports/
logs/
backups/

# Eski / yedek klasörler (refactor sonrası)
demo/

# Merge/revert arşivleri (diskte kalır, repoya girmez)
data/external/
data/raw/*.pre_merge_*.json
data/raw/chatbot_dataset.*.json
!data/raw/chatbot_dataset.json

# ── Üretilmiş veri & indeks (scripts/build_index ile oluşur) ─
data/processed/embeddings.npz
data/processed/index_meta.json
data/processed/chatbot_dataset_augmented.json
data/processed/*.tmp.npz
data/processed/*.pre_kurumsal_merge_*.json
data/processed/*.pre_merge_*.json

# Yerel pg_dump çıktısı (~70 MB; diskte kalır, repoya girmez)
db/vector_index_dump.sql

# ── Model / ONNX önbellek ───────────────────────────────────
**/_reranker_onnx_cache/
.huggingface/
huggingface/
.cache/

# ── Postman / API dokümantasyon araçları ────────────────────
db_api/postman_collection.json
db_api/postman_environment.json
db_api/generate_postman.py
db_api/POSTMAN.md

# ── Tek seferlik debug / doğrulama scriptleri ───────────────
cleanup.py
verify_fixes.py

# ── Statik test sayfaları (runtime: widget_test.html yeterli) ─
static/index.html
static/empty_test.html
static/widget_toggle_contrast_test.html

# ── Ölü kod (varsa) ─────────────────────────────────────────
src/router_server.py
src/frontend.py
run_router_8001.py
app.py

# ── OS / IDE ────────────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/
